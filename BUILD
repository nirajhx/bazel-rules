# For Bazel Docker
# Blank BUILD file for https://github.com/bazelbuild/rules_docker
# Known Issues: [...]
# Ensure your project has a BUILD or BUILD.bazel file at the top level.
# This can be a blank file if necessary.
# Otherwise you might see and error that looks like:
# Unable to load package for //:WORKSPACE: BUILD file not found in any of the following directories.
#
# For Bazel Go
# Add a file named BUILD.bazel in the root directory of your project.
# You'll need a build file in each directory with Go code,
# but you'll also need one in the root directory, even if your project doesn't have Go code there.
